package criteriaOfAcceptance;

/**
 * 
 * Crear un mapa con un tamaño de 5x4, se crea un rectángulo nº1 (posición (0,1), tamaño (1,2), tipo de estructura (acero)), en el mapa.
 
 _Si se logra crear el rectángulo, las posiciones que ocupa deberán quedar ocupadas.
 _Si se quiere crear un rectángulo nº2 (posición (0,2), tamaño (1,2), tipo de estructura (acero)), en el mismo mapa en el que 
   se ingresó el objeto nº1, en este caso, no se tiene que poder, porque el mapa ya está ocupado en la posición (0,2) 
   por el rectángulo nº1.
 _Si se quiere crear un rectángulo (posición (4,3), tamaño (1,2), tipo de estructura (acero)) me voy de rango con el tamaño de 
  la estructura, deberá devolver error.
 _Si deseo meter otra estructura, ejemplo tipo de estructura (árbol), que no soporte  (solo acero por ahora) debe devolver error. 

 *
 */
public class UserStory_03 {

}
